const liquidityPool = {
  // The amount of the token asset currently held in the pool.
  assetReserve: 1000,

  // The amount of USDC currently held in the pool.
  usdcReserve: 200000,

  // The total supply of LP tokens that represent liquidity provider ownership.
  lpTokenSupply: 1000,
};

module.exports = liquidityPool;
